# JavaScript Function Expressions

Things I learnt:

- Function expressions is like storing a function in a variable.
- Arrow functions is a shorter way of writing function expressions.
- The difference between a parameter and argument is that parameter is the placeholder while the argument is the actual value given.
- The shorter arrow-function syntax is for example const add = (a, b) => a * b;