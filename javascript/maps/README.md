## JavaSript Map

Things I learned:

- What map() does: It helps create a new array from an existing array.
- What happens to each item: The function given to map() runs on each item, and the returned value is added to the new array.
- Difference between map() and forEach(): map() creates a new array with the returned results, while forEach() simply runs a function on each item in the array.