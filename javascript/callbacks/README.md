# JavaScript Function Callbacks

Things I learnt:

- A callback is like passing a function into another function as argument.
- We pass greet instead of greet() because, greet says "Here's the function" while greet() says "Run this function".
- When a function is called, the arguments are passed into the corresponding parameters, such as name and callback.